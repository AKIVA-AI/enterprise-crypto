/**
 * Hook for accessing trading mode context
 * Re-exports from context for convenience
 */

export { useTradingMode } from '@/contexts/TradingModeContext';
export type { TradingMode, VenueConfig, ArbitrageStrategy } from '@/lib/tradingModes';
